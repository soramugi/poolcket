== poolcket

http://poolcket.herokuapp.com

  heroku create
  heroku config:add CONSUMER_KEY=YOUR_CONSUMER_KEY
  heroku config:add SECRET_KEY_BASE=$(rake secret)
  git push heroku master
  heroku run rake migrate
